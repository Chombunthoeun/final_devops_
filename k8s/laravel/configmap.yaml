apiVersion: v1
kind: ConfigMap

metadata:
  name: laravel-config
  namespace: school-system

data:
  APP_NAME: Laravel
  APP_ENV: local
  APP_DEBUG: "true"
  APP_URL: http://localhost

  DB_CONNECTION: mysql
  DB_HOST: db
  DB_PORT: "3306"
  DB_DATABASE: laravel_app
  DB_USERNAME: laravel